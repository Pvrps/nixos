# This file is encrypted by install.sh using the host's age key.
# Secrets are added via: just secrets windwaker
#
# Populated by install.sh:
#   purps-password: <sha-512 hash>
#
# Added manually post-install via `just secrets windwaker`:
#   github-ssh-key: |
#     -----BEGIN OPENSSH PRIVATE KEY-----
#     ...
#     -----END OPENSSH PRIVATE KEY-----
